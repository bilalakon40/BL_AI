# Quiz 01: اختبر نفسك!

# 1. ما الأمر الذي يستخدم لطباعة نص على الشاشة؟
# أ) echo()
# ب) print()    ✓
# ج) write()
# د) say()

# 2. أي من التالي ليس نوع بيانات في Python؟
# أ) int
# ب) string
# ج) float
# د) char    ✓ (لا يوجد char منفصل، النصوص كلها str)

# 3. ما ناتج: print(2 + 3 * 4)؟
# أ) 20
# ب) 14    ✓ (الضرب أولاً)
# ج) 24
# د) خطأ

# 4. كيف تطلب إدخال من المستخدم؟
# أ) scan()
# ب) get()
# ج) input()    ✓
# د) read()

# 5. ما الفرق بين = و == في Python؟
# = : تعيين قيمة (assignment)
# == : مقارنة (comparison)
